interface:
  display_name: "Smartedu"
  short_description: "Repo-specific patterns and workflows for smartedu"
  default_prompt: "Use the smartedu repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true